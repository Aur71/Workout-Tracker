<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Workout_Tracker.ViewModel"
             xmlns:model="clr-namespace:Workout_Tracker.Model"
             x:Class="Workout_Tracker.View.SelectMusclesPage"
             x:DataType="vm:SelectMusclesViewModel"
             BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource OffBlack}}"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto,Auto,*,Auto">

        <!-- Header -->
        <Grid Padding="20,20,20,0" ColumnDefinitions="Auto,*,Auto">
            <Label Text="Cancel"
                   FontSize="15"
                   TextColor="{StaticResource Primary}"
                   VerticalOptions="Center">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding CancelCommand}" />
                </Label.GestureRecognizers>
            </Label>
            <Label Grid.Column="1"
                   Text="Select Muscles"
                   FontSize="17"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" />
            <Label Grid.Column="2"
                   Text="     "
                   FontSize="15" />
        </Grid>

        <!-- Search Bar -->
        <Border Grid.Row="1"
                Margin="20,16,20,0"
                StrokeThickness="1"
                Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                Padding="12,0"
                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource CardBackground}}">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10" />
            </Border.StrokeShape>
            <Grid ColumnDefinitions="Auto,*">
                <Label Text="&#x1F50D;"
                       FontSize="16"
                       VerticalOptions="Center"
                       TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}" />
                <Entry Grid.Column="1"
                       Placeholder="Search muscles..."
                       Text="{Binding SearchText}"
                       PlaceholderColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                       Margin="8,0,0,0" />
            </Grid>
        </Border>

        <!-- Muscle List -->
        <CollectionView Grid.Row="2"
                         ItemsSource="{Binding Muscles}"
                         Margin="20,16,20,0">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="12" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:MuscleSelection">
                    <Border StrokeThickness="{Binding IsSelected, Converter={StaticResource SelectedStrokeThicknessConverter}}"
                            Stroke="{Binding IsSelected, Converter={StaticResource SelectedBorderConverter}}"
                            BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource CardBackground}}"
                            Padding="16">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12" />
                        </Border.StrokeShape>

                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" RowSpacing="8">

                            <!-- Muscle Name (tappable for toggle) -->
                            <Label Text="{Binding Muscle.Name}"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SelectMusclesViewModel}}, Path=ToggleMuscleCommand}"
                                                          CommandParameter="{Binding}" />
                                </Label.GestureRecognizers>
                            </Label>

                            <!-- Checkmark circle -->
                            <Border Grid.RowSpan="2"
                                    Grid.Column="1"
                                    StrokeThickness="2"
                                    Stroke="{Binding IsSelected, Converter={StaticResource SelectedBorderConverter}}"
                                    WidthRequest="24"
                                    HeightRequest="24"
                                    BackgroundColor="{Binding IsSelected, Converter={StaticResource SelectedCheckBgConverter}}"
                                    VerticalOptions="Center">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12" />
                                </Border.StrokeShape>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SelectMusclesViewModel}}, Path=ToggleMuscleCommand}"
                                                          CommandParameter="{Binding}" />
                                </Border.GestureRecognizers>
                                <Label Text="&#x2713;"
                                       FontSize="14"
                                       IsVisible="{Binding IsSelected}"
                                       TextColor="{StaticResource Black}"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center" />
                            </Border>

                            <!-- Role buttons (only visible when selected) -->
                            <HorizontalStackLayout Grid.Row="1" Spacing="8"
                                                    IsVisible="{Binding IsSelected}">
                                <!-- Primary button -->
                                <Border StrokeThickness="0"
                                        Padding="8,4"
                                        BackgroundColor="{Binding Role, Converter={StaticResource RoleToPrimaryBgConverter}}">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="4" />
                                    </Border.StrokeShape>
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SelectMusclesViewModel}}, Path=SetRoleCommand}"
                                                              CommandParameter="{Binding}" />
                                    </Border.GestureRecognizers>
                                    <Label Text="Primary"
                                           FontSize="11"
                                           FontAttributes="{Binding Role, Converter={StaticResource RoleToPrimaryFontAttrConverter}}"
                                           TextColor="{Binding Role, Converter={StaticResource RoleToPrimaryTextConverter}}" />
                                </Border>

                                <!-- Secondary button -->
                                <Border StrokeThickness="0"
                                        Padding="8,4"
                                        BackgroundColor="{Binding Role, Converter={StaticResource RoleToSecondaryBgConverter}}">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="4" />
                                    </Border.StrokeShape>
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SelectMusclesViewModel}}, Path=SetRoleCommand}"
                                                              CommandParameter="{Binding}" />
                                    </Border.GestureRecognizers>
                                    <Label Text="Secondary"
                                           FontSize="11"
                                           FontAttributes="{Binding Role, Converter={StaticResource RoleToSecondaryFontAttrConverter}}"
                                           TextColor="{Binding Role, Converter={StaticResource RoleToSecondaryTextConverter}}" />
                                </Border>
                            </HorizontalStackLayout>

                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Add Button -->
        <Border Grid.Row="3"
                StrokeThickness="0"
                Padding="20,12,20,24"
                BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource OffBlack}}">
            <Button Text="{Binding ButtonText}"
                    Command="{Binding ConfirmCommand}"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="{StaticResource Black}"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="52"
                    CornerRadius="12" />
        </Border>

    </Grid>

</ContentPage>
