<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Workout_Tracker.ViewModel"
             xmlns:model="clr-namespace:Workout_Tracker.Model"
             x:Class="Workout_Tracker.View.AddExercisesPage"
             x:DataType="vm:AddExercisesViewModel"
             BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource OffBlack}}"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto,Auto,*,Auto">

        <!-- Header -->
        <Grid Padding="20,20,20,0" ColumnDefinitions="Auto,*,Auto">
            <Label Text="Cancel"
                   FontSize="15"
                   TextColor="{StaticResource Primary}"
                   VerticalOptions="Center">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding CancelCommand}" />
                </Label.GestureRecognizers>
            </Label>
            <Label Grid.Column="1"
                   Text="Add Exercises"
                   FontSize="17"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" />
            <Label Grid.Column="2"
                   Text="     "
                   FontSize="15" />
        </Grid>

        <!-- Search Bar -->
        <Border Grid.Row="1"
                Margin="20,16,20,0"
                StrokeThickness="1"
                Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}"
                Padding="12,0"
                BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource CardBackground}}">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10" />
            </Border.StrokeShape>
            <Grid ColumnDefinitions="Auto,*">
                <Label Text="&#x1F50D;"
                       FontSize="16"
                       VerticalOptions="Center"
                       TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}" />
                <Entry Grid.Column="1"
                       Placeholder="Search exercises..."
                       Text="{Binding SearchText}"
                       PlaceholderColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}"
                       Margin="8,0,0,0" />
            </Grid>
        </Border>

        <!-- Exercise List -->
        <CollectionView Grid.Row="2"
                         ItemsSource="{Binding Exercises}"
                         Margin="20,16,20,0">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="12" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:ExerciseSelection">
                    <Border StrokeThickness="{Binding IsSelected, Converter={StaticResource SelectedStrokeThicknessConverter}}"
                            Stroke="{Binding IsSelected, Converter={StaticResource SelectedBorderConverter}}"
                            BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource CardBackground}}"
                            Padding="16">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12" />
                        </Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:AddExercisesViewModel}}, Path=ToggleExerciseCommand}"
                                                  CommandParameter="{Binding}" />
                        </Border.GestureRecognizers>

                        <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto" RowSpacing="6">
                            <Label Text="{Binding Exercise.Name}"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" />

                            <!-- Checkmark circle -->
                            <Border Grid.RowSpan="2"
                                    Grid.Column="1"
                                    StrokeThickness="2"
                                    Stroke="{Binding IsSelected, Converter={StaticResource SelectedBorderConverter}}"
                                    WidthRequest="24"
                                    HeightRequest="24"
                                    BackgroundColor="{Binding IsSelected, Converter={StaticResource SelectedCheckBgConverter}}"
                                    VerticalOptions="Center">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12" />
                                </Border.StrokeShape>
                                <Label Text="&#x2713;"
                                       FontSize="14"
                                       IsVisible="{Binding IsSelected}"
                                       TextColor="{StaticResource Black}"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center" />
                            </Border>

                            <HorizontalStackLayout Grid.Row="1" Spacing="8">
                                <Border StrokeThickness="0"
                                        Padding="6,2"
                                        BackgroundColor="{Binding Exercise.TypeBadgeBg}">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="4" />
                                    </Border.StrokeShape>
                                    <Label Text="{Binding Exercise.TypeDisplay}"
                                           FontSize="11"
                                           TextColor="{Binding Exercise.TypeBadgeTextColor}" />
                                </Border>
                                <Label Text="{Binding Exercise.PrimaryMuscle}"
                                       FontSize="13"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                                       VerticalOptions="Center" />
                            </HorizontalStackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Add Button -->
        <Border Grid.Row="3"
                StrokeThickness="0"
                Padding="20,12,20,24"
                BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource OffBlack}}">
            <Button Text="{Binding ButtonText}"
                    Command="{Binding ConfirmCommand}"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="{StaticResource Black}"
                    FontAttributes="Bold"
                    FontSize="16"
                    HeightRequest="52"
                    CornerRadius="12" />
        </Border>

    </Grid>

</ContentPage>
